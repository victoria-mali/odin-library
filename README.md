# odin-library
1. Create a constructor for books (that creates books as objects)
2. Create a function that takes some arguments that creates a book from those arguments and stores teh new book in teh array
3. Each book should have a unique ID
4. Write a function that loops through the array and displays each book on the page
5. Add a new book button that brings up a form to add a new book to the library
6. Add a button to delete a book from the library
7. Add a butoon on each book to change its read status